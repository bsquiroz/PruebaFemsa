/* eslint-disable prettier/prettier */

export interface PropsMoves {
  createdAt?: string;
  product: string;
  price: string;
  image: string;
  is_redemption: boolean;
  id: string;
}

export interface PropsMoveDetail {
  product: string;
  image: string;
  is_redemption: boolean;
  price: string;
}
